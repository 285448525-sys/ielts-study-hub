# 写作真题模块 + 语料库简化 · 改动总结

## 一、做了什么
按你的要求把「作文」大模块的第 4 个 tab「默写」替换成「写作真题」，并简化了语料库页面、补上了「看中文写英文」默写功能。

### 1. 写作真题模块（替换原「默写」tab）
- **文件**：`writing.html`、`js/writing.js`、`js/writing_prompts.js`（新增）
- 写作页 tab 顺序现为：模板库 / 语料库 / AI 评分 / **写作真题**
- 题库来源：你把的 `26年6月雅思写作（纸笔+机考）预测.docx`，已提取为 `data/writing_prompts.json`（**70 道大作文 + 25 道小作文**），并内联成 `js/writing_prompts.js` 供前端直接加载（避免 fetch 跨域问题）。
- 功能：
  - 题目列表按「大作文 / 小作文」分组，顶部可筛选（全部 / 大作文 / 小作文）。
  - 点开任意一道题 → 进入练习界面，**右下角计时器自动开始**（仅计时、不强制限时，可暂停/继续）。
  - 写完点「AI 按官方 4 维度评分」→ 复用官方 TR/CC/LR/GRA 评分逻辑给出预估分 + 长难句分析 + 改进建议。
- 原「默写」tab 的 DOM 与 `js/dictation.js` 引用已从 writing.html 移除（不再加载无用代码）。

### 2. 语料库简化 + 默写功能
- **文件**：`corpus.html`、`js/corpus.js`（既有逻辑复用）
- 语料库第三个子 tab 改为「**默写（看中文写英文）**」：
  - 顶部：单条添加（英文 + 中文）+ 批量导入（自动分隔中英文）。
  - 语料表：**左中文、右英文** 的紧凑表格（你要求的「简化」）。
  - 「开始默写（看中文写英文）」按钮 → 左侧中文提示、右侧填英文，提交后 AI 批改（已有 `startWrite`/`gradeWrite` 逻辑，本次补齐了缺失的 HTML 骨架）。
- 原语料库里错误的「写作真题」占位（无功能）已删除。

### 3. 计时 bug 修复（上一轮已部署）
- 修复 `js/timer.js`：倒计时归零后自动结算并写入学习记录（之前只响铃不入库）。提交 `9d45a37`。

## 二、关键决策
- 真题选题直接采用你给的 docx 预测题，未另行编造；小作文部分原文为英文/片段，按原文展示。
- 写作真题「计时」按你要求做的是纯记录、不强制限时（右下角独立计时器，可暂停）。

## 三、注意 / 待跟进
- `data/writing_prompts.json` 与 `js/writing_prompts.js` 是同一份题库的两份副本（JS 内联用于本地预览，JSON 留作备份/后续扩展）。如需更新题目，改 JSON 后重新生成 JS 即可。
- 尚未做浏览器实测（agent-browser 正在后台安装 Chromium），页面已通过语法校验且本地 HTTP 200 可访问。
- 下一步：等你确认页面外观/交互是否符合预期；如需调整真题评分提示语、题目筛选、计时样式或语料表布局，告诉我即可。
